[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "xet_verify"
version = "0.1.0"
description = "Recompute Xet content-addresses using xet-core, pinned to hf-xet v1.6.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "xet_verify"
